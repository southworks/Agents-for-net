# Microsoft.Agents.Builder.Dialogs

## About

Provides Bot Framework SDK dialog support for agents built with the Microsoft 365 Agents SDK. Enables reuse of existing Bot Framework dialogs — including waterfall, prompt, and component dialogs — within an `AgentApplication`.
